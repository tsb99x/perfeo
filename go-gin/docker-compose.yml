version: "3"
include:
  - ../docker-compose.yml
services:

  api:
    build: .
    environment:
      GIN_MODE: "release"
      PORT: "8080"
      DB_URI: "mongodb://db:27017"
    ports:
      - "8080:8080"
    depends_on:
      - db

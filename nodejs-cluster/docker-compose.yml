version: "3"
include:
  - ../docker-compose.yml
services:

  api:
    build: .
    environment:
      NODE_ENV: "production"
      DEBUG: "*"
      PORT: "8080"
      DB_URI: "mongodb://db:27017/test"
    ports:
      - "8080:8080"
    depends_on:
      - db
